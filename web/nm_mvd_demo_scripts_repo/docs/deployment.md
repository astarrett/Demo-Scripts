# Deployment Notes

- Keep image assets in `web/shared/assets/`
- Publish individual experiences from their own folders
- Avoid hardcoding sample data in production builds
- Replace `config.sample.js` with runtime values from Genesys participant data or query parameters
